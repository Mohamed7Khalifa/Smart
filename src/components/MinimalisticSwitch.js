export default function MinimalisticSwitch({ onChange, ...otherProps }) {

    return (<View style={{ alignItems: 'center', display: 'flex', flexDirection: 'row', justifyContent: 'space-between' }}>
    </View>)
}